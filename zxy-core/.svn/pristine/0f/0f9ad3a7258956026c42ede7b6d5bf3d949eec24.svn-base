package com.jtang.core.cache;

/**
 * 缓存管理监听接口
 * @author 0x737263
 *
 */
public interface CacheListener {
	
	/**
	 * 下线后，达成清理规则，则执行该方法
	 * @param actorId
	 * @return	剩余数量
	 */
	int cleanCache(long actorId);
}
