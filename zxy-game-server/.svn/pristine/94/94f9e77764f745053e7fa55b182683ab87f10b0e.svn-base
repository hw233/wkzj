package com.jtang.gameserver.component.listener;

/**
 * 角色退出监听接口
 * 继承该事件后，角色退出时(SessionClose)会触发登陆方法处理事务
 * @author 0x737263
 *
 */
public interface ActorLogoutListener  {

	/**
	 * 退出时触发
	 * @param actorId	当前角色id
	 */
	void onLogout(long actorId);
}
