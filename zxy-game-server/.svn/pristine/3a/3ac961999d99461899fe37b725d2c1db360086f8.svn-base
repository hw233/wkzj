package com.jtang.gameserver.module.sign.dao;

import com.jtang.gameserver.dbproxy.entity.Sign;

public interface SignDao {

	/**
	 * 获取签到信息
	 * @param actorId
	 * @return
	 */
	Sign get(long actorId);
	
	/**
	 * 更新签到信息
	 * @param sign
	 * @return
	 */
	void update(Sign sign);
}
