package com.jtang.gameserver.admin.facade.impl;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import com.jtang.core.result.Result;
import com.jtang.gameserver.admin.facade.VampiirMaintianFacade;
import com.jtang.gameserver.module.user.facade.ActorFacade;
import com.jtang.gameserver.module.vampiir.facade.VampiirFacade;

@Component
public class VampiirMaintianFacadeImpl implements VampiirMaintianFacade {

	@Autowired
	ActorFacade actorFacade;
	
	@Autowired
	VampiirFacade vampiirFacade;
	
//	@Autowired
//	VampiirDao vampiirDao;
	
	@Override
	public Result modifyVampiirLevel(long actorId, int level) {
//		Vampiir vampiir = vampiirFacade.get(actorId);
//		if (vampiir == null) {
//			return Result.valueOf(VAMPIIR_LEVEL_UP_FAIL);
//		}
//		
//		int oldLevel = vampiir.level;
//		int nextLevel = level;
//		
//		if (nextLevel > VampiirService.getMaxVampiirLevel()) {
//			return Result.valueOf(VAMPIIR_LEVEL_UP_IS_MAX);
//		}
//
//		if (!vampiirDao.update(actorId, nextLevel)) {
//			return Result.valueOf(VAMPIIR_LEVEL_UP_FAIL);
//		}
//		
//		Actor actor = actorFacade.getActor(actorId);
//		GameOssLogger.vampiirUpgrade(actor.uid, actor.platformType, actor.channelId, actor.serverId, actorId, oldLevel, nextLevel);
		
		return Result.valueOf();
	}

}
